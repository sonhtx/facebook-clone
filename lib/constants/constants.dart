import 'package:flutter/material.dart';

class Constants {
  static const Color CYAN = Colors.cyan;
  static const Color DARK_BLUE = Color(0xff0165E1);
  static const defaultPadding = EdgeInsets.symmetric(
    horizontal: 15,
    vertical: 10,
  );
// Add more color constants as needed
}
